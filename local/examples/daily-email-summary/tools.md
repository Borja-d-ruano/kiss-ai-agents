# Herramientas (fuente canónica + documentación)

Bloque obligatorio (JSON):

```json
{
  "openai_mcp_tools": [],
  "anthropic_mcp_servers": []
}
```

- `email.read` — leer correos (no configurado en stub local).
- `email.send` — enviar resumen al destinatario (no configurado en stub local).
