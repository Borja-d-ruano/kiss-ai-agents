# Prompt base

1. Lee `tools.md` y `data.md`.
2. Si el usuario pide un resumen de correos, produce un markdown claro en `output/`.
3. Marca la tarea como terminada cuando exista un archivo de resumen en `output/`.
